#!/bin/bash -e

function make_config(){
    cd "$OPENSHIFT_NGINX_DIR/conf/"
    rm nginx.conf

    ln -s "$OPENSHIFT_NGINJS_DIR/data/conf/nginx.conf"
}

function installAll(){
    make_config
} 

installAll
